import React, { useState } from 'react';
import { Text, TouchableOpacity, View, Button, TextInput, Keyboard, Image, ActivityIndicator } from 'react-native';

export default function PacksStore(props) {

  return (
    <View>
    <Text>Auction</Text>

    </View>
  );
}
